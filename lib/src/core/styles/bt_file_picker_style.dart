import 'package:flutter/material.dart';

class BTFilePickerStyle {
  Color bgColor;
  Color foregroundColor;
  Color borderColor;
  Color splashColor;

  BTFilePickerStyle({
    required this.bgColor,
    required this.foregroundColor,
    required this.borderColor,
    required this.splashColor,
  });
}
